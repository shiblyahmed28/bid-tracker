import { Placeholder } from "../components/Placeholder";

export function NotificationsPage() {
  return (
    <Placeholder
      title="Notifications"
      description="Your notification feed and per-field subscription settings — arrives in a later phase."
    />
  );
}
