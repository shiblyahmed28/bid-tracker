import { Placeholder } from "../components/Placeholder";

export function SessionsPage() {
  return (
    <Placeholder
      title="Login history"
      description="Your sign-in history and active sessions — arrives in a later phase."
    />
  );
}
