import { Placeholder } from "../../components/Placeholder";

export function AuditLogPage() {
  return (
    <Placeholder
      title="Audit log"
      description="Every tracked change, filterable by user, action, bid and date — arrives in a later phase."
    />
  );
}
