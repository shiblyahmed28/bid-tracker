import { Placeholder } from "../../components/Placeholder";

export function SyncHistoryPage() {
  return (
    <Placeholder
      title="Sync history"
      description="Past sync runs, quarantined rows and conflict resolution — arrives in a later phase."
    />
  );
}
