import { Placeholder } from "../components/Placeholder";

export function DashboardPage() {
  return (
    <Placeholder
      title="Dashboard"
      description="The executive dashboard — KPIs, trend, result mix, submission runway — arrives in a later phase."
    />
  );
}
