import { Placeholder } from "../components/Placeholder";

export function BidsPage() {
  return (
    <Placeholder
      title="All bids"
      description="The register — search, filters, column picker, pagination and PDF export — arrives in a later phase."
    />
  );
}
